services:
  nginx:
    image: nginx:alpine
    ports:
      - "127.0.0.1:8099:80"
    volumes:
      - ./book:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
