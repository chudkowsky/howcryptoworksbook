services:
  nginx:
    image: nginx:alpine
    ports:
      - "0.0.0.0:8002:80"
    volumes:
      - ./book:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
